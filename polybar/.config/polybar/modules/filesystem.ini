[module/filesystem]
type = internal/fs
interval = 25

mount-0 = /

label-mounted = "  %percentage_used:0:2%%"

label-unmounted = %mountpoint% not mounted
label-unmounted-foreground = ${colors.disabled}